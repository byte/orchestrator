{
  "name": "orchestrator-plugin",
  "version": "0.4.0",
  "private": true,
  "type": "module",
  "description": "A durable Claude supervisor for a routed GPT-5.6 Codex worker pool.",
  "license": "Apache-2.0",
  "engines": {
    "node": ">=18.18"
  },
  "scripts": {
    "test": "node --test \"tests/**/*.test.mjs\""
  }
}
